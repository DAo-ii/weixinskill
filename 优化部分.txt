优化清单
安全

SECRET_KEY 移除硬编码默认值，强制从 .env 读取
CORS 从 * 全开改为白名单
新建 .gitignore，防止 .env、venv、node_modules 泄漏
下载接口增加路径遍历防护
删除登录页明文演示凭据
架构

数据库 Session 从 3 个路由文件各自 next() 统一到 core/database.py，用 FastAPI 依赖注入
generate_skill 不再自行创建 session，由调用方传入，避免泄漏
新增 core/logging_config.py，print() 替换为 Python logging
新增 TEMPLATE_DIR_MAP 统一管理模板 ID 到目录的映射
废弃 API 修复

declarative_base() → DeclarativeBase
datetime.utcnow() → datetime.now(timezone.utc)
前端关键修复

next.config.js API 代理端口 8081 → 8000（修复登录 500）
Create 页面从 setTimeout 模拟改为真实调用后端 API
API 客户端新增 token 自动附加和 401 自动跳转登录
新建 types/index.ts 集中类型定义，消除 any
模板列表从硬编码改为 API 动态获取
新增 error.tsx 错误边界组件